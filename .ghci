:set -fwarn-unused-binds -fwarn-unused-imports
:set -isrc
:load Main

:{
:def opt const $ return $ unlines
        [":!mkdir obj"
        ,":!mkdir obj\\o2"
        ,":!ghc -O2 -isrc --make Main -outputdir obj/o2 -o obj/o2/main"
        ,":!obj\\o2\\main"]
:}
:}
