:set -fwarn-unused-binds -fwarn-unused-imports
:set -isrc
:load Main

:{
:def opt const $ return $ unlines
        [":!mkdir obj"
        ,":!ghc -O2 -isrc --make Main.hs -outputdir obj -o obj/main"
        ,":!obj\\main"]
:}
