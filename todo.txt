
Before beta:

* Fix module searching
* Add easy to restrict/include package: things on the LHS
* Implement type search
* Schedule the updating script with cron

After beta:

* Deal with unicode properly
* Add is:package is:module
